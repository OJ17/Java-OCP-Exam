package ch2DesignPatternsAndPrinciples;


// Factory pattern. This is another creational design pattern
// it abstracts the object creation process into a single class,
// why is it required?
// this has the benefit of standardizing and therefore simplifying
// object creation
public abstract class FactoryPattern {
    /* public static food getFood(String animalName){
    switch(animalName) {
        case "Dog": return new DogFood(2);
        case "Cat": return new CatFood(1);
        case "Rabbit": return new Grass(100);
        case "Horse": return new hay(100);
        
        }

    public class ZooKeeper {
        public static void main(String[] args) {
        final Food food = FoodFactory.getFood("polar bear");
        food.consumed();
        }
    }

    }
    */
}
